[project]
name = "ml-api"
version = "0.1.0"
description = "FastAPI: análisis DSP + clasificador de mood + router pc/app"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "scikit-learn",
    "joblib",
    "librosa",
    "soundfile",
    "numpy",
    "python-multipart",
]

[tool.uv]
package = false
